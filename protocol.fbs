include "client_join.fbs";
include "player_movement.fbs";

namespace sanhok.protocol;

union ProtocolType {
    ClientJoin,
    PlayerMovement,
}

table Protocol {
    protocol: ProtocolType;
}

root_type Protocol;